<% include ../_partials/head %>
  <% include ../_partials/messages %>
  <main>
    <% if (!images.length) { %>
      <h2>No images</h2>
    <% } else { %>
      <% for (let image of images) { %>
        <section class="image">
          <a href="<%= `/image/${agent.getAgentDirectory()}/${image}` %>">
            <img src="<%= `/uploads/${agent.getAgentDirectory()}/${image}` %>" />
          </a>
        </section>
      <% } %>
    <% } %>
  </main> 
<% include ../_partials/footer %>

