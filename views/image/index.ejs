<% include ../_partials/head %>
  <% include ../_partials/messages %>
  <main>
    <section class="deep-link">
      <a href="bpe://<%= token %>"><img src="/images/bpe-logo.png"></a>
    </section>
    <% if (!images.length) { %>
      <h2>No images</h2>
    <% } else { %>
      <% include ./_pager %>
      <% for (let image of images) { %>
        <section class="image">
          <a href="<%= `/image/${image}` %>">
            <img src="<%= `/uploads/${image}` %>" />
          </a>
        </section>
      <% } %>
      <% include ./_pager %>
    <% } %>
  </main> 
<% include ../_partials/footer %>

